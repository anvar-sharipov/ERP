// import ErrorStatusMessage from "../ErrorStatusMessage";


// interface StatusProps {
//     isLoading: boolean
//   error: any; // Сюда передаем весь объект ошибки
//   text403?: string;
// }


// const ErrorMsg = ({isLoading, error, text403}: StatusProps) => {
//     if (isLoading) return <div>Загрузка...</div>;
//   if (error) return <ErrorStatusMessage error={error} text403={text403} />;

// }

// export default ErrorMsg