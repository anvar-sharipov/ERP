# backend/companies/urls.py

from django.urls import path

app_name = 'companies'

urlpatterns = [
    # Например, здесь можно смотреть список компаний, доступных пользователю
    # path('list/', CompanyListView.as_view(), name='list'),
    
    
]